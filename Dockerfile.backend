FROM golang:1.21-alpine

WORKDIR /app

# 必要なパッケージをインストール
RUN apk add --no-cache git sudo npm

# Go依存関係をコピー
COPY backend/go.mod backend/go.sum ./
RUN go mod download

# アプリケーションコードをコピー
COPY backend/ .

# アプリケーションをビルド
RUN go build -o main .

EXPOSE 8070

CMD ["./main"]
